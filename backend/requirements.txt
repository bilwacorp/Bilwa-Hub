fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
pydantic==2.7.1
pydantic-settings==2.3.0
python-multipart==0.0.9
httpx==0.27.0
greenlet==3.0.3
redis[asyncio]>=5.0.0
casbin>=1.36.0
casbin_async_sqlalchemy_adapter>=1.7.0
cryptography>=42.0.0

# services/notifications — email + WhatsApp alerts, delivered via a Celery
# worker (see docker-compose.yml's celery-worker service). See
# services/notifications/README.md.
celery[redis]>=5.4.0
aiosmtplib>=3.0.1
jinja2>=3.1.4
email-validator>=2.1.1

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
aiosqlite>=0.20.0

# app/workflow, app/rules, app/approvals — embedded BPMN workflow +
# approval engine (ported from PoultryPro-CBF), gating deployment
# renew/suspend/change-plan. See CLAUDE.md's "Workflow & approval engine".
SpiffWorkflow==3.1.2
rule-engine>=5.0.2,<6
