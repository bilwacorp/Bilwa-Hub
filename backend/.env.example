DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/bilwacorp_hub
DB_SSL_MODE=disable
SECRET_KEY=change-me-32-chars-minimum-random
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

BACKEND_CORS_ORIGINS=["http://localhost:5174"]
REDIS_URL=redis://localhost:6379

LOG_LEVEL=INFO
APP_ENV=development
FRONTEND_URL=http://localhost:5174

# Required. Generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
HUB_ENCRYPTION_KEY=

# ── Only the root docker-compose.yml uses these (Dokploy Compose deploy, or
#    a local `docker compose up`). Ignored when running uvicorn directly. ──
# POSTGRES_PASSWORD=change-me
# HUB_PUBLIC_URL=https://hub.bilwacorp.example
